{
  "name": "tts-lab",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "TTS Lab — first-class benchmark app comparing neural/browser TTS engines for The Inclusionist.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@jrocha-io/tts": "^0.2.0",
    "@jrocha-io/audio": "^0.1.0",
    "@jrocha-io/logging": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.0.0",
    "vitest": "^4.0.0"
  }
}
